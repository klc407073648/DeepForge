name = "testsite"
match = ["example.com"]
priority = 5

[crawl]
content_selectors = [".article-content"]
title_selector = "h1.title"
remove_selectors = [".sidebar"]
include_patterns = ["*/article/*"]
exclude_patterns = ["*/tag/*"]
min_content_chars = 50
title_cleanup = " - Example$"
